apiVersion: v1
kind: Service
metadata:
  name: {{ include "coupon-service.fullname" . }}
  labels:
    {{- include "coupon-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "coupon-service.selectorLabels" . | nindent 4 }}
